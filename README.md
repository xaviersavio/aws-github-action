# Simple Website

Open `index.html` in a browser to view the site. From PowerShell run:

```powershell
Start-Process .\website\index.html
```

Edit `website/index.html` and `website/styles.css` to customize content and styles.
